# MSDS 120개 학습·검증 보고서

## 데이터 구성

- 총 120개, SHA-256 중복 0개, PDF 헤더 오류 0개
- GS칼텍스 20개, 노루페인트 8개, 일신 37개, S-OIL 55개
- 외부 서비스로 업로드하지 않고 로컬에서만 처리

출처 URL, 제조사, 파일 크기, SHA-256, 로컬 경로는
`validation_corpus/manifest.json`에 기록한다.

## 분할

`validation_corpus/split_100_20.json`에 제조사 층화 고정 분할을 저장한다.
SHA-256 기반 고정 순위를 사용하므로 같은 매니페스트에서는 항상 같은 결과가 나온다.

| 제조사 | 학습 | 검증 |
|---|---:|---:|
| GS칼텍스 | 17 | 3 |
| 노루페인트 | 7 | 1 |
| 일신 | 31 | 6 |
| S-OIL | 45 | 10 |
| 합계 | 100 | 20 |

## 원본 대조 기준

- W-1: 결과가 비어 있지 않고 원문 1항에 존재하는지 확인
- W-2: 중복 그림문자와 유해정보 대비 비정상적인 빈 결과 확인
- W-3: 원문 2항의 신호어와 비교
- W-4/W-5: 원문 2항과 결과의 H/P 코드 집합 비교
- W-6: 원문 1항에 공급자 정보가 있는데 결과가 비는지 확인
- M-1~M-5: 대응 원문 절이 있을 때 빈 결과인지 확인하고 다음 절 혼입 검사
- M-4: 보호구 명칭 이외의 착용·설치·관리 설명문 혼입 검사

## 학습 세트 개선

초기 학습 결과는 55/100 통과였다. 원문 대조 후 다음을 개선했다.

1. `제품명` 외에 `품명` 및 다음 줄에 배치된 제품명을 인식
2. 인라인 `2) 신호어: 경고` 형식을 인식
3. `· 신호어` 다음 줄의 `해당사항없음`을 인식
4. 표 추출 순서가 뒤집힌 `해당사항없음 · -신호어`를 인식
5. 구형 PDF의 반복된 절 제목을 인식
6. M-4를 보호구 명칭만 남기도록 제한
7. 원문 1항의 공급자 전화·긴급연락 번호를 W-6에 포함하고 FAX는 제외

개선 후 학습 결과는 100/100 통과, 오류 CSV 0행이다.

## 격리 검증 결과

- 최초 검증: 19/20 통과
- 최초 불일치: S-OIL `B.S.Wax` W-2 1건
- 원인: PDF 글머리표 `l` 때문에 `신호어 / 해당사항없음` 연결 실패
- 개선 후 검증: 20/20 통과
- 단위/API 테스트: 21개 통과

최초 불일치는 `validation_corpus/validation_mismatches_initial.csv`에 보존한다.
현재 결과는 `validation_corpus/validation_mismatches.csv`와 각 report JSON에 기록한다.

## 재현 명령

```bash
../env/bin/python scripts/build_validation_corpus.py
../env/bin/python scripts/run_training_validation.py --phase train --rebuild-split
../env/bin/python scripts/run_training_validation.py --phase validation
../env/bin/python -m pytest -q
```

## 한계

이 검증은 원문 PDF의 텍스트·코드·절 경계와 결과를 자동 대조하는 회귀 검증이다.
모든 문구의 법적·의미적 정확성을 사람이 라벨링한 정답 데이터와 비교한 것은 아니다.
스캔 전용 PDF, 손상된 글꼴, 새 그림문자 표현은 별도 수동 검토가 필요할 수 있다.
